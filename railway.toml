[build]
  builder = "NIXPACKS"

[deploy]
  startCommand = "node Cipher/server/index.js"
  healthcheckPath = "/api/health"
  healthcheckTimeout = 60
  restartPolicyType = "ON_FAILURE"
  restartPolicyMaxRetries = 3
